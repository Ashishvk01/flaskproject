""""
Without __init__.py:

Python may treat the folder as a normal folder

Imports can fail or behave unexpectedly (especially in older Python versions)

With __init__.py:

Python knows the folder is meant for importing code

"""